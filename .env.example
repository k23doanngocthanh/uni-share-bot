# Production Environment Variables for GitHub Pages
# These should be set in GitHub repository secrets

# Supabase Configuration
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Telegram Bot Configuration (embedded in code)
# VITE_TELEGRAM_BOT_TOKEN= (not needed, hardcoded in TelegramService)
# VITE_TELEGRAM_CHAT_ID= (not needed, hardcoded in TelegramService)

# Build Configuration
NODE_ENV=production
VITE_BASE_URL=/uni-share-bot/
